import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'jw971r14',
    dataset: 'production',
  },
  autoUpdates: true,
  deployment: {
    appId: 'u7lp0so5rfa5qt4ff7xp7ile',
  },
})

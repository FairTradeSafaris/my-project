import ClientHomeContent from "./ClientHomeContent";

export default async function ClientHomePage() {
  // Optionally, you can still get the user/email here if needed, but not required for trips.
  return (
    <>
      <ClientHomeContent />
    </>
  );
}
